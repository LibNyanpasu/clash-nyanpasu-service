[package]
name = "nyanpasu-logging"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
chrono.workspace = true
serde.workspace = true
serde_json = "1"
lasso = "0.7.3"
rustc-hash = "2"
specta = { version = "^2.0.0-rc.25", features = ["derive"], optional = true }

[features]
specta = ["dep:specta"]
